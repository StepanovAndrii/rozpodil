import { AutoFormatDateDirective } from './auto-format-date.directive';

describe('AutoFormatDateDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoFormatDateDirective();
    expect(directive).toBeTruthy();
  });
});
